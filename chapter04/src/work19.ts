export {};

// 型エイリアスを利用して「Monster」型を作成します
// 以下の定義に従って、「Monster」型を作成します
// 「string」型のプロパティ「name」
// 「number」型のプロパティ「hp」
// 「number」型のプロパティ「attackpoint」
// 「string」型のプロパティ「type」

// Monster型の変数「dragon」を作成して、各プロパティに適切な値を代入します
// 実行時には以下のコメントを削除します
/*
const dragon: Monster = {
  name: 'ファイヤードラゴン',
  hp: 200,
  attackpoint: 50,
  type: '炎',
};
// 変数「dragon」を表示します
console.log(dragon);
*/
