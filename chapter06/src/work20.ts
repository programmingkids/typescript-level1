export {};

// 以下のような関数が定義されています
const multiply = (a: number, b: number, c: number): number => {
  return a * b * c;
};

// 以下の部分に処理を作成します。これより上部は修正しません
// 関数「multiply」を呼び出して、引数として、3, 5, 2を渡します
// 関数「multiply」の戻り値を表示します
console.log(multiply(3, 5, 2));
