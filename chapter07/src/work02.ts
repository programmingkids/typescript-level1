export {};

// 以下のような型エイリアスが宣言されています
type MovieGenre = 'SF' | 'Action' | 'Comedy' | 'Love';

// 以下の部分に処理を作成します。これより上部は修正しません
// MovieGenre型の変数「muvie」を宣言して、「Comedy」を代入します

// MovieGenre型の変数「movie」を表示します
