export {};

// 以下のような関数が定義されています
const calc = (a: number, b: number, c: number): number => {
  return (a * b) / c;
};

// 以下の部分に処理を作成します。これより上部は修正しません
// 関数「calc」を呼び出して、引数として、4, 6, 3を渡します
// 関数「calc」の戻り値を表示します
console.log(calc(4, 6, 3));
