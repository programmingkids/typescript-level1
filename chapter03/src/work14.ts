export {};

// 型エイリアスの宣言
type UserName = string;

// UserName型の変数
const name: UserName = 'Bob';
console.log(name);

// 型エイリアスの宣言
type UserAge = number;

// UserAge型の変数
const age: UserAge = 17;
console.log(age);
